package types

var (
	REQUEST_FLAG       = byte(1)
	RESPONSE_FLAG      = byte(2)
	EVENT_FLAG         = byte(3)
	REQUEST_ZSTD_FLAG  = byte(4)
	RESPONSE_ZSTD_FLAG = byte(5)
	EVENT_ZSTD_FLAG    = byte(6)
)
