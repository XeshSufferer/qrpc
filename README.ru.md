[**English version**](README.md)

# qrpc

[![Go Version](https://img.shields.io/badge/Go-1.26.2-blue.svg)](go.mod)
[![License](https://img.shields.io/badge/License-Apache%202.0-green.svg)](LICENSE)

**qrpc** — высокопроизводительный RPC-фреймворк поверх QUIC (транспорт HTTP/3). Использует protocol buffers с ускорением vtprotobuf, lock-free мультиплексирование стримов и агрессивный object pooling для минимальных задержек и максимальной пропускной способности.

---

## Возможности

- **Транспорт QUIC** — UDP + TLS 1.3, отсутствие head-of-line блокировок, встроенное шифрование.
- **Protobuf + vtprotobuf** — компактная бинарная сериализация с zero-copy marshal.
- **Мультиплексирование стримов** — N предварительно открытых QUIC-стримов на соединение, lock-free atomic round-robin балансировщик.
- **Object Pooling** — `sync.Pool` для буферов кодировщика, объектов запросов/ответов и каналов ответов — минимизация нагрузки на GC.
- **Sharded Concurrent Map** — 256-секционная конкурентная карта для O(1) диспетчеризации по `request_id`.
- **Простой API** — `NewServer` → `AddHandler`, `NewClient` → `SendRequest`.

---

## Быстрый старт

### Сервер

```go
package main

import (
  "crypto/tls"
  "log"
  "github.com/XeshSufferer/qrpc"
  "github.com/XeshSufferer/qrpc/protos/pb/gen"
)

func main() {
  tlsConfig := /* *tls.Config */
  server, err := qrpc.NewServer("0.0.0.0:8081", tlsConfig)
  if err != nil {
    log.Fatal(err)
  }
  server.AddHandler("echo", func(req *gen.Request, resp *gen.Response) {
    resp.Body = req.Body
    resp.Code = 0
  })
  select {}
}
```

### Клиент

```go
package main

import (
  "context"
  "crypto/tls"
  "log"
  "github.com/XeshSufferer/qrpc"
)

func main() {
  tlsConfig := /* *tls.Config */
  client, err := qrpc.NewClient(context.Background(), "127.0.0.1:8081", tlsConfig, 1)
  if err != nil {
    log.Fatal(err)
  }
  resp, err := client.SendRequest(
    context.Background(),
    []byte("echo"), []byte("hello qrpc"), nil,
  )
  if err != nil {
    log.Fatal(err)
  }
  log.Printf("Response: %s", resp.Body)
}
```

---

## Проводной протокол

### Формат фрейма

```
┌─────────────────────────────────────────────┐
│  4 байта: длина payload (big-endian uint32)  │
├─────────────────────────────────────────────┤
│  1 байт:  флаг                               │
│           REQUEST  = 1                       │
│           RESPONSE = 2                       │
│           EVENT    = 3                       │
├─────────────────────────────────────────────┤
│  N байт: protobuf Request / Response         │
└─────────────────────────────────────────────┘
```

- `payload length` = 1 (флаг) + protobuf байты
- vtprotobuf использует `MarshalToSizedBufferVT` (обратная запись в предварительно выделенный буфер)

### Сообщения (protobuf)

```protobuf
message Request {
  uint64 request_id = 3;
  bytes  headers    = 1;
  bytes  method     = 2;
  bytes  body       = 4;
}

message Response {
  uint64 request_id = 3;
  uint32 code       = 5;
  bytes  headers    = 1;
  bytes  method     = 2;
  bytes  body       = 4;
}
```

Запросы сопоставляются с ответами через случайный `uint64` `request_id`.

---

## Архитектура

```
┌───────────────┐     QUIC (UDP)      ┌───────────────┐
│   Client      │ ◄─────────────────► │   Server      │
│               │  TLS 1.3, ALPN     │               │
│  ┌─────────┐  │  "qrpc"            │  ┌─────────┐  │
│  │ Sharded │  │                     │  │Handlers │  │
│  │  Map    │  │                     │  │  Map    │  │
│  └────┬────┘  │                     │  └─────────┘  │
│  ┌────▼────┐  │  N предв.          │  ┌─────────┐  │
│  │Multiplex│  │  открытых стримов  │  │  Stream │  │
│  │   -er   │──┤───────────────────►│  │  Read   │  │
│  └────┬────┘  │                     │  │  Cycle  │  │
│  ┌────▼────┐  │                     │  └─────────┘  │
│  │Balancer │  │  atomic round-robin │               │
│  │(lockfree)│  │                     │               │
│  └─────────┘  │                     │               │
└───────────────┘                     └───────────────┘
```

**Клиент.** При `NewClient` мультиплексор открывает N QUIC-стримов (по умолчанию 32) и запускает по горутине чтения на стрим. `SendRequest` генерирует случайный `request_id`, сохраняет `chan *Response` в sharded map, кодирует запрос и отправляет на стрим, полученный от round-robin балансировщика. Горутина чтения декодирует входящие фреймы, находит `request_id` в карте и отправляет ответ в канал.

**Сервер.** `NewServer` запускает QUIC-слушатель. Каждое принятое соединение получает горутину, принимающую стримы. Каждый стрим выполняет цикл чтения: декодирует фрейм, диспетчеризует к зарегистрированному обработчику, кодирует ответ и отправляет обратно.

---

## Производительность

Все бенчмарки запущены на localhost с фиксированным payload 1 KB, если не указано иное. Результаты получены с помощью фреймворка в `stress_tester/` — подробнее в разделе [Тестирование](#тестирование).

### Базовая задержка (1 worker, 1 stream)

| Метрика | qrpc       | gRPC       | qrpc vs gRPC |
|---------|------------|------------|--------------|
| RPS     | **11,703** | 8,683      | **+34.8%**   |
| Avg     | **59.8 µs**| 81.0 µs    | **-26.2%**   |
| P50     | **58.0 µs**| 73.7 µs    | **-21.3%**   |
| P95     | **84.8 µs**| 124 µs     | **-31.6%**   |
| P99     | **109 µs** | 236 µs     | **-53.7%**   |
| Success | 100%       | 100%       | =            |

### Конкурентная нагрузка (100 workers, 32 streams, 1–32 KB random)

| Метрика | qrpc        | gRPC         | qrpc vs gRPC |
|---------|-------------|--------------|--------------|
| RPS     | 16,314      | **28,294**   | -42.3%       |
| Avg     | 5.06 ms     | **2.89 ms**  | +75%         |
| P50     | 3.99 ms     | **2.65 ms**  | +51%         |
| P95     | 12.8 ms     | **5.22 ms**  | +145%        |
| P99     | 18.0 ms     | **7.09 ms**  | +154%        |
| Success | 100%        | 100%         | =            |

> gRPC лучше справляется с конкурентностью на чистой сети благодаря оптимизациям HTTP/2. Блокировки на уровне стримов в qrpc — узкое место при высокой конкурентности на localhost.

### Смешанная нагрузка (50 workers, 64 streams, 80% малых + 20% 1–11 MB)

| Метрика    | qrpc         | gRPC         |
|------------|--------------|--------------|
| RPS        | 130          | **1,848**    |
| Avg        | 303 ms       | **16.8 ms**  |
| P50        | 161 ms       | **8.2 ms**   |
| P95        | 1.11 s       | **67.7 ms**  |
| P99        | 1.48 s       | **174 ms**   |
| **Success**| **100%**     | **86.1%**    |

> qrpc обрабатывает все запросы успешно при смешанной нагрузке; gRPC теряет ~14% запросов. Изоляция стримов QUIC предотвращает HOL-блокировки, от которых страдает HTTP/2.

### Чувствительность к потерям (100 workers, 32 streams, 1–16 KB random)

| Метрика | qrpc       | gRPC         | qrpc vs gRPC |
|---------|------------|--------------|--------------|
| RPS     | 36,644     | **38,419**   | -4.6%        |
| Avg     | 2.24 ms    | **2.14 ms**  | +4.7%        |
| P50     | **1.86 ms**| 1.91 ms      | **-2.6%**    |
| P95     | 5.10 ms    | **4.07 ms**  | +25%         |
| P99     | 7.44 ms    | **5.72 ms**  | +30%         |
| Success | 100%       | 100%         | =            |

### Масштабирование RTT (50 workers, 16 streams, 1 KB fixed)

| Метрика | qrpc          | gRPC          | qrpc vs gRPC |
|---------|---------------|---------------|--------------|
| RPS     | **102,443**   | 75,560        | **+35.6%**   |
| Avg     | **391 µs**    | 541 µs        | **-27.7%**   |
| P50     | **325 µs**    | 444 µs        | **-26.8%**   |
| P95     | **868 µs**    | 1.13 ms       | **-23.2%**   |
| P99     | **1.26 ms**   | 1.94 ms       | **-35.1%**   |
| Success | 100%          | 100%          | =            |

### Экстремальные условия (64 workers, 150 ms RTT, 10% потерь пакетов)


| Метрика | qrpc         | gRPC         | qrpc vs gRPC |
|---------|--------------|--------------|--------------|
| RPS     | **83.5**     | 64.9         | **+28.6%**   |
| Avg     | **590 ms**   | 825 ms       | **-28.5%**   |
| P50     | **527 ms**   | 715 ms       | **-26.3%**   |
| P95     | **1.11 s**   | 1.70 s       | **-34.7%**   |
| P99     | **1.44 s**   | 2.14 s       | **-32.5%**   |
| Success | 100%         | 100%         | =            |

> При экстремальной деградации сети (150 ms RTT, 10% потерь) qrpc превосходит gRPC на **~29% по пропускной способности** и **~26% по медианной задержке**.

---

## Итоги

| Сценарий              | Победитель    | Ключевой вывод                               |
|------------------------|---------------|----------------------------------------------|
| Базовая задержка       | **qrpc**      | +35% RPS, -26% средняя задержка              |
| Конкурентная нагрузка  | **gRPC**      | +73% RPS на чистом localhost                 |
| Смешанная нагрузка     | **qrpc**      | 100% успеха vs 86% (HOL блокировки)          |
| Чувствительность к потерям | ~равно   | Сопоставимо на чистой сети                   |
| Масштабирование RTT    | **qrpc**      | +36% RPS, -28% средняя задержка              |
| Экстремальные условия  | **qrpc**      | +29% пропускная способность, -26% P50        |

qrpc показывает лучшие результаты там, где это важнее всего: в реальных сетях с задержками, потерями, смешанной нагрузкой и HOL-чувствительными сценариями. В идеальных лабораторных условиях зрелый стек HTTP/2 от gRPC может обеспечить более низкие хвостовые задержки при высокой конкурентности.

---

## Установка

```bash
go get github.com/XeshSufferer/qrpc
```

Требуется Go 1.26.2+.

---

## Тестирование

### Модульные бенчмарки

```bash
go test -bench=. -benchmem ./...
```

### Фреймворк нагрузочного тестирования

```bash
cd stress_tester
go build -o qrpc-stress .

# Запуск сервера для бенчмарков
sudo ./qrpc-stress server -addr 127.0.0.1:8081

# Запуск сценария
sudo ./qrpc-stress run -scenario baseline_latency -profile clean,wifi,lte -system qrpc -addr 127.0.0.1:8081

# Полный набор тестов
sudo ./run_all.sh --duration 10s --warmup 3s
```

### HTML-отчёт

```bash
python3 analyze.py results --html report.html
```

Сетевые профили применяются через Linux `tc` netem (требуется `sudo`).

---

## Сетевые профили

| Профиль    | RTT    | Джиттер | Потери | Пропускная | Описание          |
|------------|--------|---------|--------|------------|-------------------|
| clean      | 1ms    | 0ms     | 0%     | 1000 Mbps  | Локальная / ЦОД   |
| wifi       | 5ms    | 2ms     | 0.1%   | 100 Mbps   | Типичный WiFi     |
| lte        | 30ms   | 10ms    | 1%     | 50 Mbps    | 4G LTE            |
| bad_lte    | 60ms   | 20ms    | 5%     | 10 Mbps    | Плохой LTE        |
| extreme    | 150ms  | 50ms    | 10%    | 5 Mbps     | Экстремальные усл.|

---

## Сценарии тестов

| Сценарий            | Workers | Streams | Payload                     | Профили                        |
|---------------------|---------|---------|-----------------------------|--------------------------------|
| baseline_latency    | 1       | 1       | 1 KB fixed                  | clean, wifi, lte               |
| concurrency_stress  | 100     | 32      | 1–32 KB random              | clean, wifi, lte, bad_lte      |
| multiplex_stress    | 50      | 64      | 80% малые + 20% 1–11 MB     | clean, lte, bad_lte            |
| loss_sensitivity    | 100     | 32      | 1–16 KB random              | все 5                          |
| rtt_scaling         | 50      | 16      | 1 KB fixed                  | все 5                          |

---

## Зависимости

| Библиотека | Версия    | Назначение                   |
|------------|-----------|------------------------------|
| quic-go    | v0.59.1   | Транспорт QUIC               |
| vtprotobuf | v0.6.0    | Быстрый marshal protobuf     |
| google.golang.org/protobuf | v1.36.11 | Среда protobuf |

---

## Лицензия

Apache License 2.0 — см. [LICENSE](LICENSE).
